// utility used if you want to expose an aggregated endpoint merging backend1 + backend2
export function mergeFlat(backend1Array, backend2obj) {
  // This is a minimal helper if you later want flat merge; for now server.js uses fetchAlbion2D.fetchPrices
  return { ok: true };
}
